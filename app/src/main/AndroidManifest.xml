<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Google Sign-In + Drive backup connector, and Microsoft/OneDrive
         connector (Settings > Account): needed for the Credential Manager /
         MSAL sign-in flows and Drive/Graph REST calls; see
         data/account/GoogleAccountManager.kt and
         data/account/MicrosoftAccountManager.kt. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Calendar Sync (Settings > Calendar Sync): read/write whatever
         Google, Office/Exchange, or local calendar the user picks on this
         device; see data/calendar/DeviceCalendarManager.kt. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- Health Connect (Android's equivalent to Apple Health) read permissions
         for the Vitals feature: see data/health/HealthConnectManager.kt.
         MedRecall only ever reads; there are no matching WRITE_* permissions. -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE" />
    <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_GLUCOSE" />
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />

    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedRecall">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MedRecall"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- MSAL's OAuth redirect comes back through this activity, the
             path is our debug-keystore signature hash, URL-encoded, computed
             per Microsoft's Android quickstart. Must match the Android
             platform config (package name com.asok.medrecall + signature
             hash) added to the Azure App Registration, or MSAL's sign-in
             will fail with a redirect_uri mismatch. See
             res/raw/msal_config.json for the same redirect_uri. -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="msauth"
                    android:host="com.asok.medrecall"
                    android:path="/xX4B7C0UGmlCJ8yMzlQQ1%2FikpYY%3D" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
